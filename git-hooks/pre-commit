#!/usr/bin/env ruby
# vim: set syntax=ruby

flag = false

files = `git diff --name-only --diff-filter=ACM`.split("\n")
files.each do |file|
  results = `git diff #{file} | grep "^\+[^+]" | grep -e "binding.pry" -e "focus: true" -e ":focus => true" -e "console.log" -e "debugger"`.split("\n").map { |r| r.sub(/^\+[\s\t]*/, '') }

  if $? == 0
    puts file
    puts results.map { |r|
      `grep -n "#{r}" #{file}`
    }.join("")
    puts "Please remove development stuff before commiting"
    flag = true
  end
end

exit 1 if flag
